{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main and pill windows",
  "windows": ["main", "pill"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
