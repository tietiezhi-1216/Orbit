# 模型能力注册表

`models.json` 是桌面端无法从渠道模型接口获得完整元数据时的内置兜底。

维护规则：

- 优先使用精确模型 ID；仅对能力长期稳定的模型家族使用前缀。
- 每条规则附官方资料 `source`，修改时同步更新根级 `updatedAt`。
- 渠道返回的模态和 `supported_parameters` 优先于本表。
- 用户在设置中进行的覆盖优先级最高，刷新模型列表不得清除。
- 新增或修改规则后，在 Rust 的 `commands::models` 测试中加入真实模型 ID。
